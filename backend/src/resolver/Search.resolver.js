export const searchResolver = {
    Mutation: {
        search: async function(_, data, { models }) {
            try {
    
            } catch (error) {
                console.error(error)
    
                return null
            }
        }
    }
}

export default searchResolver
