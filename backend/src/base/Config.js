export const CONFIG = {
    DATABASE: [
        'postgres',
        'postgres',
        '',
        {
            dialect: 'postgres',
            host: 'localhost',
            underscored: true,
            logging: false
        }
    ],
    URL: {
        SERVER: 'http://localhost:3001/'
    }
}

export default CONFIG
