export const CONFIG = {
    DATABASE: [
        'test',
        'postgres',
        '',
        {
            dialect: 'postgres',
            host: 'localhost',
            underscored: true
        }
    ]
}

export default CONFIG
